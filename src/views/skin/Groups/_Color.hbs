{{!--
Light Mode
--}}
<Group description="Light">
  {{! Used in: --primary }}
  {{variable "primary" description="Accents" type="color" value="#14b8a6"}}
  {{! Used in: --secondary }}
  {{variable "secondary" description="Alternate Color" type="color" value="#0a0a0a"}}
  {{! Used in: --bg-body }}
  {{variable "body.background.color" description="Body background color" type="color" value="#fff"}}
  {{! Used in: --bg-template }}
  {{variable "bg.template" description="Content color" type="color" value="#fff"}}
  {{! Used in: --bg-inset }}
  {{variable "bg.inset" description="Post background color" type="color" value="#f5f5f5"}}
  {{! Used in: --bg-subtle }}
  {{variable "bg.subtle" description="Background color (Top)" type="color" value="#fff"}}
  {{! Used in: --color-title }}
  {{variable "color.title" description="Title color" type="color" value="#222"}}
  {{! Used in: --color-text }}
  {{variable "color.text" description="Text color" type="color" value="#373737"}}
  {{! Used in: --color-subtle }}
  {{variable "color.subtle" description="Subtle color" type="color" value="#a3a3a3"}}
  {{! Used in: --color-border }}
  {{variable "color.border" description="Border color" type="color" value="#e5e5e5"}}
  {{! Used in: --color-header-title }}
  {{variable "color.header.title" description="Header color" type="color" value="#0a0a0a"}}
  {{! Used in: --color-header-subtitle }}
  {{variable "color.header.subtitle" description="Description Color" type="color" value="#404040"}}
</Group>
{{!--
Dark Mode
--}}
<Group description="Dark">
  {{! Used in: --primary }}
  {{variable "dark.primary" description="Accents" type="color" value="#14b8a6"}}
  {{! Used in: --secondary }}
  {{variable "dark.secondary" description="Alternate Color" type="color" value="#737373"}}
  {{! Used in: --bg-body }}
  {{variable "dark.body.background.color" description="Body background color" type="color" value="#0a0a0a"}}
  {{! Used in: --bg-template }}
  {{variable "dark.bg.template" description="Content color" type="color" value="#0a0a0a"}}
  {{! Used in: --bg-inset }}
  {{variable "dark.bg.inset" description="Post background color" type="color" value="#171717"}}
  {{! Used in: --bg-subtle }}
  {{variable "dark.bg.subtle" description="Background color (Top)" type="color" value="#262626"}}
  {{! Used in: --color-title }}
  {{variable "dark.color.title" description="Title color" type="color" value="#fff"}}
  {{! Used in: --color-text }}
  {{variable "dark.color.text" description="Text color" type="color" value="#e5e5e5"}}
  {{! Used in: --color-subtle }}
  {{variable "dark.color.subtle" description="Subtle color" type="color" value="#525252"}}
  {{! Used in: --color-border }}
  {{variable "dark.color.border" description="Border color" type="color" value="#404040"}}
  {{! Used in: --color-header-title }}
  {{variable "dark.color.header.title" description="Header color" type="color" value="#fff"}}
  {{! Used in: --color-header-subtitle }}
  {{variable "dark.color.header.subtitle" description="Description Color" type="color" value="#e5e5e5"}}
</Group>
